import 'package:flutter/material.dart';

class SearchGroupTrip extends StatelessWidget {
  const SearchGroupTrip({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}